<Window x:Class="ERP.UI.WPF.Views.OrderPositionEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pozycja zamówienia"
        Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Pozycja zamówienia" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="id_towaru:" Margin="0,5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            <Button Grid.Row="0" Grid.Column="2" Content="Wybierz towar..." Command="{Binding SelectProductCommand}" Margin="5" Padding="10,4"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="towar_nazwa_draco:" Margin="0,5"/>
            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding ProductNameDraco, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="ilosc_zamawiana:" Margin="0,5"/>
            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding OrderQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="cena_zakupu:" Margin="0,5"/>
            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding PurchasePrice, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="stawka_vat:" Margin="0,5"/>
            <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding VatRate, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="uwagi:" Margin="0,5" VerticalAlignment="Top"/>
            <TextBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Margin="5"
                     TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="data_dostawy_pozycji:" Margin="0,5"/>
            <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding DeliveryDateIntText, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Command="{Binding SaveCommand}" Padding="16,5" Margin="0,0,8,0"/>
            <Button Content="Anuluj" Command="{Binding CancelCommand}" Padding="16,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
