<UserControl x:Class="ERP.UI.WPF.Views.KontrahentPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="400">
    <StackPanel>
        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,4" Padding="4"
                 ToolTip="Szukaj po nazwie lub emailu"/>
        <ComboBox ItemsSource="{Binding Kontrahenci}"
                  SelectedItem="{Binding SelectedKontrahent, Mode=TwoWay}"
                  DisplayMemberPath="Nazwa"
                  IsEditable="False"
                  MinWidth="200"
                  Padding="4"/>
    </StackPanel>
</UserControl>
