<Window x:Class="ERP.UI.WPF.Views.CompanySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybór firmy" 
        Height="400" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tytuł -->
        <TextBlock Grid.Row="0" 
                   Text="Wybór firmy" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10"/>

        <!-- Informacja o użytkowniku -->
        <TextBlock Grid.Row="1" 
                   Text="{Binding UserFullName, StringFormat='Zalogowany jako: {0}'}" 
                   FontSize="14" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Lista firm -->
        <ListBox Grid.Row="2" 
                 ItemsSource="{Binding Companies}"
                 SelectedItem="{Binding SelectedCompany}"
                 Margin="0,0,0,20">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding Name}" 
                                   FontSize="14" 
                                   FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Przyciski -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Wybierz" 
                    Command="{Binding SelectCommand}"
                    IsDefault="True"
                    Padding="20,5"
                    Margin="0,0,10,0"
                    MinWidth="100"/>
            <Button Content="Anuluj" 
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Padding="20,5"
                    MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
