<UserControl x:Class="ERP.UI.WPF.Views.OperatorCompanyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="OperatorFirma (nowy moduł)" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="ID firmy:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding CompanyId, UpdateSourceTrigger=PropertyChanged}" Width="80" Padding="5" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Label Content="ID operatora (0=wszystkie):" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding FilterOperatorId, UpdateSourceTrigger=PropertyChanged}" Width="80" Padding="5" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Ładuj listę" Command="{Binding LoadCommand}" Padding="10,5" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <CheckBox Content="Pokaż nieaktywne" IsChecked="{Binding IncludeInactive, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ID operatora" Binding="{Binding UserId}" Width="90"/>
                <DataGridTextColumn Header="ID firmy" Binding="{Binding CompanyId}" Width="80"/>
                <DataGridTextColumn Header="Rola" Binding="{Binding RoleId}" Width="60"/>
                <DataGridCheckBoxColumn Header="Aktywny" Binding="{Binding IsActive}" Width="60" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="3" Header="Akcje" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Dodaj" Command="{Binding AddCommand}" Padding="10,5" Margin="0,0,5,0"/>
                <Button Content="Edytuj" Command="{Binding EditCommand}" Padding="10,5" Margin="0,0,5,0"/>
                <Button Content="Dezaktywuj" Command="{Binding DeactivateCommand}" Padding="10,5"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
