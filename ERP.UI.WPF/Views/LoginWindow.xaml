<Window x:Class="ERP.UI.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logowanie" 
        Height="380" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tytuł -->
        <TextBlock Grid.Row="0" 
                   Text="Logowanie do systemu ERP" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Login -->
        <TextBlock Grid.Row="1" 
                   Text="Login:" 
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" 
                 x:Name="LoginTextBox"
                 Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15"
                 Padding="5"
                 FontSize="14"/>

        <!-- Hasło -->
        <TextBlock Grid.Row="3" 
                   Text="Hasło:" 
                   Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" 
                     x:Name="PasswordBox"
                     Margin="0,0,0,10"
                     Padding="5"
                     FontSize="14"/>

        <!-- Baza danych -->
        <TextBlock Grid.Row="5" Text="Środowisko bazy:" Margin="0,0,0,5"/>
        <Grid Grid.Row="6" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" 
                      ItemsSource="{Binding EnvironmentOptions}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Value"
                      SelectedValue="{Binding SelectedEnvironment, UpdateSourceTrigger=PropertyChanged}"
                      Padding="5" FontSize="12" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="Test połączenia" 
                    Command="{Binding TestConnectionCommand}"
                    Padding="10,5" Margin="10,0,0,0" MinWidth="110"/>
        </Grid>

        <!-- Przyciski -->
        <StackPanel Grid.Row="8" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Zaloguj" 
                    Command="{Binding LoginCommand}"
                    IsDefault="True"
                    Padding="20,5"
                    Margin="0,0,10,0"
                    MinWidth="100"/>
            <Button Content="Anuluj" 
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Padding="20,5"
                    MinWidth="100"/>
        </StackPanel>

        <!-- Komunikat błędu -->
        <TextBlock Grid.Row="9" 
                   Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"
                   Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>
