<Window x:Class="ERP.UI.WPF.Views.KontrahentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edycja kontrahenta" Height="360" Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Typ"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Typ}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Nazwa"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Nazwa}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Email"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Email}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Telefon"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Telefon}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Miasto"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Miasto}"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,8,6" VerticalAlignment="Center" Text="Waluta"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,6" Text="{Binding Waluta}"/>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,0,8,0" Content="Zapisz" Command="{Binding SaveCommand}"/>
            <Button Width="90" Content="Anuluj" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
